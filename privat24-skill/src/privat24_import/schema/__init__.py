"""SQL migration files for the privat_* schema group.

Files are read at runtime via ``importlib.resources.files(...)`` from
``privat24_import.core.store`` so the package works in both source-tree
layout and an installed wheel.
"""
